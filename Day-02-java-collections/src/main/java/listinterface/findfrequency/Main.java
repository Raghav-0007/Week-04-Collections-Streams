package listinterface.findfrequency;

public class Main {
    public static void main(String[] args) {

        // List of elements
        String[] arr = {"apple", "banana", "apple", "orange"};

        // call the method to find the frequency of elements
        System.out.println(FindFrequency.findFrequency(arr));
    }
}
